# Python-
Python code I developed during data analysis project and timeseries analysis.

It reads a csv file excluding label axis (vertical and horizontal).

It converts time from form HH:MM:SS to seconds.

It fits the data to a sinusoidal-linera trend.

Prints the plot (raw data and fitting) and the fitting parameters calculated.

